extends ../layout/layout.pug

block title
    - var title = "Homepage"

block page_content
    section.header-container
        .header-bg
        header.header
            .main-container
                h1.logo
                    a(href="#") #[i.fas.fa-map-marked] #[=siteTitle]
                nav.nav.main-nav
                    .nav__item.nav__item--active
                        a(href="#") Home
                        nav.nav__sub
                            each index in [0,1,2]
                                .nav__item
                                    a(href="#") Item #{index}
                    .nav__item
                        a(href="#") Pages
                nav.nav.side-nav
                    .nav__item
                        a(href="#") Sign Up
                    .nav__item.nav__item--featured
                        a(href="#") Sign In
        .hero
            .main-container
                .hero-desc
                    h2 Find amazing places for the right price.
                    h3 Discover your next travel.
                label.hero-search(for="hero-search")
                    input#hero-search(type="text", placeholder="What are you looking for?")
                    button #[i.fas.fa-search] Search
    p Aliqua commodo laboris eiusmod velit sunt et. Laborum officia sunt mollit excepteur adipisicing anim esse.